{
  "watch": {
    "build": {
      "patterns": ["src/**"],
      "extensions": "md,yml"
    }
  },
  "scripts": {
    "serve": "http-server ./build/latest",
    "watch_docfx": "npm-watch build",
    "build": "docfx build .\\src\\latest_docfx.json"
  }
  
}