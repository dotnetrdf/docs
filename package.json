{
  "watch": {
    "build": {
      "patterns": ["src/**"],
      "extensions": "md,yml"
    }
  },
  "scripts": {
    "watch_docfx": "npm-watch build",
    "build": "docfx build .\\src\\latest_docfx.json --serve"
  }
  
}